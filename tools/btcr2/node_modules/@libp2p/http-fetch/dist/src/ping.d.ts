import { type Libp2p, type PeerId } from '@libp2p/interface';
import { type Multiaddr } from '@multiformats/multiaddr';
import type { HTTP } from './index.js';
export declare const PING_PROTOCOL_ID = "/http-ping/1";
/**
 * Serve a ping request
 *
 * @param req - An HTTP Request object
 * @returns a Response object
 */
export declare function servePing(req: Request): Promise<Response>;
/**
 * Send a ping request to a peer
 *
 * @param node - a libp2p node
 * @param peerIdOrMultiaddr - Target peer
 */
export declare function sendPing(node: Libp2p<{
    http: HTTP;
}>, peerIdOrMultiaddr: PeerId | Multiaddr): Promise<void>;
//# sourceMappingURL=ping.d.ts.map